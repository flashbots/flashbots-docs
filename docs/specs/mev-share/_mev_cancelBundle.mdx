```typescript
{
    jsonrpc: "2.0",
    id: string | number,
    method: "mev_cancelBundle",
    params: [
        {
            replacemendUuid: string,
        },
        Array<string>, /* Array of builders, optional argument */
    ]
}
```
